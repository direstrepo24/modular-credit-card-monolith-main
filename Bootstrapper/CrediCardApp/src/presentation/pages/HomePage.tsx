import { CardMain } from "@presentation/components/card/CardMain";
import { Component } from "react";

export default class HomePage extends Component {
    render() {
        return (
            <CardMain></CardMain>
        );
    }
}